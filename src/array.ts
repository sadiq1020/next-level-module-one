// example 1
const names: string[] = ["Abul", "Kabul", "Babul"]; // : string[] means it is a array of strings
names.map(name => name.toUpperCase()) // name. er pore sob string er jinispatra gulo dicche

// example 2
const rollNumbers: number[] = [55, 5, 103]; // : number[] means it is a array of numbers
rollNumbers.map(rollNumber => rollNumber.toFixed) // rollNumber. er pore sob number er jinispatra gulo dicche

// example 3
const mixed = ["Sajid", 55, false]; // any type of data can be kept here
mixed.push(77); // any type of data can be pushed here

// example 4: Tupple (Special type of array)
const user: [number, string] = [516, 'Sadiq']; // Only number and string can be kept/pushed here and the pattern is also fixed.




